DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha
DB_NAME=biblioteca_db

# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=sua_chave_secreta_forte_aqui

# Nunca ativar em produção
FLASK_DEBUG=false

# Credenciais do admin
ADMIN_USERNAME=admin
# Gere com: python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('sua_senha'))"
ADMIN_PASSWORD_HASH=hash_gerado_aqui
